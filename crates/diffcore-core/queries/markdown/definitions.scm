; Markdown — a markup format with no executable symbol definitions.
; See `queries/typescript/definitions.scm` for the convention overview.
;
; Intentionally empty so the standard extractor produces zero definitions.
