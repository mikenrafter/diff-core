; HTML — there are no symbol definitions in HTML in the programming-
; language sense. Custom element names and id/class attributes are
; not extracted here because they are not callable symbols.
; See `queries/typescript/definitions.scm` for the convention overview.
;
; Intentionally empty so the standard extractor produces zero definitions.
